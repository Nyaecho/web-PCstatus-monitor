# 前端所需后端 WebSocket 数据格式

本文档描述 `前端/` 仪表盘在真机模式下期望后端通过 WebSocket 返回的数据格式。

## 连接信息

- 默认地址：`ws://localhost:8765`
- 协议：WebSocket
- 数据编码：JSON 字符串
- 后端建议只保留一个活跃客户端；前端会在断线后自动重连。

## 前端发送给后端的控制消息

前端连接成功后，以及用户调整采样间隔时，会发送以下消息：

```json
{
  "type": "set_interval",
  "value": 1
}
```

字段说明：

| 字段 | 类型 | 必需 | 说明 |
| --- | --- | --- | --- |
| `type` | string | 是 | 固定为 `set_interval` |
| `value` | number | 是 | 采样间隔，单位秒。当前前端选项通常为 `0.5`、`1`、`3` |

## 后端返回消息总结构

后端返回的每条消息都应是如下结构之一：

```ts
type ServerMessage = StaticInfoMessage | MetricsMessage;
```

### 1. 静态系统信息：`static_info`

后端应在 WebSocket 建立后优先发送一次，用于填充设备基础信息。

```json
{
  "type": "static_info",
  "data": {
    "os": "Windows 11",
    "hostname": "DESKTOP-EXAMPLE",
    "cpu_model": "AMD Ryzen 7 7840HS",
    "cpu_cores": 16,
    "cpu_freq": "4200.0 MHz",
    "ram_total_gb": 31.75,
    "disk_total_gb": 953.87,
    "up_time": "02:44:12",
    "boot_time": "2026-05-27 09:15:30"
  }
}
```

字段说明：

| 字段 | 类型 | 必需 | 单位/格式 | 说明 |
| --- | --- | --- | --- | --- |
| `type` | string | 是 | - | 固定为 `static_info` |
| `data.os` | string | 是 | - | 操作系统名称和版本 |
| `data.hostname` | string | 是 | - | 主机名 |
| `data.cpu_model` | string | 是 | - | CPU 型号 |
| `data.cpu_cores` | number | 是 | 个 | 逻辑核心数 |
| `data.cpu_freq` | string | 是 | 建议如 `4200.0 MHz` | CPU 频率描述 |
| `data.ram_total_gb` | number | 是 | GB | 内存总量 |
| `data.disk_total_gb` | number | 是 | GB | 磁盘总量 |
| `data.up_time` | string | 是 | `HH:MM:SS` 或可读时长 | 系统运行时长 |
| `data.boot_time` | string | 是 | `YYYY-MM-DD HH:mm:ss` | 启动时间 |

TypeScript 结构：

```ts
interface SystemInfo {
  os: string;
  hostname: string;
  cpu_model: string;
  cpu_cores: number;
  cpu_freq: string;
  ram_total_gb: number;
  disk_total_gb: number;
  up_time: string;
  boot_time: string;
}
```

### 2. 实时指标：`metrics`

后端应按当前采样间隔持续发送实时指标。

```json
{
  "type": "metrics",
  "data": {
    "cpu_percent": 18.4,
    "cpu_cores_percent": [12, 24, 8, 32, 16, 20, 10, 24],
    "ram_percent": 41.2,
    "ram_used_gb": 9.89,
    "ram_total_gb": 24.0,
    "disk_percent": 54.8,
    "disk_used_gb": 280.57,
    "disk_total_gb": 512.0,
    "net_sent_speed_kb": 45.4,
    "net_recv_speed_kb": 182.9,
    "gpu_percent": 24.0,
    "gpu_temp": 45,
    "cpu_temp": 42,
    "fan_speed_1": 32,
    "fan_speed_2": 38,
    "gpu_mem_percent": 28.5,
    "processes": [
      {
        "pid": 1234,
        "name": "example.exe",
        "cpu_percent": 2.1,
        "memory_percent": 0.8,
        "memory_used_mb": 256.5
      }
    ],
    "timestamp": "14:06:12"
  }
}
```

字段说明：

| 字段 | 类型 | 必需 | 单位/格式 | 说明 |
| --- | --- | --- | --- | --- |
| `type` | string | 是 | - | 固定为 `metrics` |
| `data.cpu_percent` | number | 是 | `%` | CPU 总占用率，建议范围 `0-100` |
| `data.cpu_cores_percent` | number[] | 建议 | `%` | 每个 CPU 逻辑核心占用率；数组长度建议等于逻辑核心数 |
| `data.ram_percent` | number | 是 | `%` | 内存占用率 |
| `data.ram_used_gb` | number | 是 | GB | 已用内存 |
| `data.ram_total_gb` | number | 是 | GB | 总内存 |
| `data.disk_percent` | number | 建议 | `%` | 磁盘占用率 |
| `data.disk_used_gb` | number | 建议 | GB | 已用磁盘容量 |
| `data.disk_total_gb` | number | 建议 | GB | 磁盘总容量 |
| `data.net_sent_speed_kb` | number | 是 | KB/s | 网络上行速度 |
| `data.net_recv_speed_kb` | number | 是 | KB/s | 网络下行速度 |
| `data.gpu_percent` | number | 可选 | `%` | GPU 占用率；无 GPU 时可返回 `0` |
| `data.gpu_temp` | number | 可选 | `°C` | GPU 温度；无法获取时可返回 `0` |
| `data.cpu_temp` | number | 可选 | `°C` | CPU 温度；无法获取时可返回 `0` |
| `data.fan_speed_1` | number | 可选 | `%` | 风扇 1 转速百分比；无法获取时可返回 `0` |
| `data.fan_speed_2` | number | 可选 | `%` | 风扇 2 转速百分比；无法获取时可返回 `0` |
| `data.gpu_mem_percent` | number | 可选 | `%` | GPU 显存占用率；无 GPU 时可返回 `0` |
| `data.processes` | ProcessInfo[] | 建议 | - | 进程列表；没有数据时返回空数组 `[]` |
| `data.timestamp` | string | 建议 | `HH:mm:ss` | 指标采样时间 |

TypeScript 结构：

```ts
interface ProcessInfo {
  pid: number;
  name: string;
  cpu_percent: number;
  memory_percent: number;
  memory_used_mb: number;
}

interface MetricData {
  cpu_percent: number;
  cpu_cores_percent: number[];
  ram_percent: number;
  ram_used_gb: number;
  ram_total_gb: number;
  disk_percent: number;
  disk_used_gb: number;
  disk_total_gb: number;
  net_sent_speed_kb: number;
  net_recv_speed_kb: number;
  gpu_percent?: number;
  gpu_temp?: number;
  cpu_temp?: number;
  fan_speed_1?: number;
  fan_speed_2?: number;
  gpu_mem_percent?: number;
  processes: ProcessInfo[];
  timestamp: string;
}
```

## 兼容性说明

当前前端对部分实时指标有兜底逻辑：

- `gpu_percent` 缺失时按 `0` 处理。
- `gpu_temp` 缺失时会根据 GPU 占用率生成估算值。
- `cpu_temp` 缺失时会根据 CPU 占用率生成估算值。
- `fan_speed_1`、`fan_speed_2` 缺失时会生成估算值。

但为了显示效果稳定，建议后端完整返回 `MetricData` 中的所有字段。尤其建议补齐：

- `cpu_cores_percent`
- `disk_percent`
- `disk_used_gb`
- `disk_total_gb`
- `processes`
- `timestamp`

## 后端最小可用返回示例

如果后端暂时无法提供全部字段，至少应返回：

```json
{
  "type": "metrics",
  "data": {
    "cpu_percent": 18.4,
    "ram_percent": 41.2,
    "ram_used_gb": 9.89,
    "ram_total_gb": 24.0,
    "net_sent_speed_kb": 45.4,
    "net_recv_speed_kb": 182.9,
    "gpu_percent": 0,
    "gpu_temp": 0,
    "cpu_temp": 0,
    "fan_speed_1": 0,
    "fan_speed_2": 0,
    "gpu_mem_percent": 0,
    "cpu_cores_percent": [],
    "disk_percent": 0,
    "disk_used_gb": 0,
    "disk_total_gb": 0,
    "processes": [],
    "timestamp": "14:06:12"
  }
}
```

## 错误与断线行为

前端当前主要依赖 WebSocket 的连接状态判断是否在线，不要求后端额外返回错误消息。若后端主动拒绝连接，浏览器会进入断开状态，前端会按重连策略重新连接。
